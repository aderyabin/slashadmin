= page_title "#{model_name} #{@object.id}"
= breadcrumbs({ :action => "index" } => model_name.pluralize)

.index-actions
  = link_to "Edit", { :action => "edit" }, :class => "btn"
  = link_to "Destroy", @object, :method => :delete, :class => "btn btn-danger"
  
= @page
